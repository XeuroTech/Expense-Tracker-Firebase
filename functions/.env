# Firebase Functions v2 auto-loads this file at deploy/runtime.
# See functions/src/mfa.js for how these are used.

# Turns on Google Authenticator (TOTP) mode for 2FA instead of email OTP.
MFA_USE_TOTP=true
